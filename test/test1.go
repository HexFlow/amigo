package main

func main() {
	var d int
	a, b := c, d
	b := [5]int{
		1,
		2,
		3,
		4,
		5,
	}
	for i := 0; i < 2; i++ {
		kk := 0
	}

	if k := 0; k != 1 {
		j := 0
	}

	for _, arg := range flag.Args() {
		if v, err := strconv.Atoi(arg); err == nil && len(arg) == 1 {
			numdays = v
		} else {
			location = arg
		}
	}
}
