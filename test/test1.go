package main

func main() {
	b := [5]int{
		1,
		2,
		3,
		4,
		5,
	}
	for i := 0; i < 2; i++ {
		k = 0
	}

	if k := 0; k != 1 {
		d = 0
	}

	for _, arg := range flag.Args() {
		if v, err := strconv.Atoi(arg); err == nil && len(arg) == 1 {
			numdays = v
		} else {
			location = arg
		}
	}
}
